import './button.css';

interface ButtonProps {
  children: string;
  style: string;
  icon: string;
  onClick?: () => void;
}

const Button = ({ children, style, icon, onClick }: ButtonProps) => {
  return <></>;
};

export default Button;
