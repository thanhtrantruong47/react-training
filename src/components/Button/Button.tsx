import './button.css';

interface ButtonProps {
  children: string;
  style: string;
  icon: string;
  onClick?: () => void;
}

const Button = (_: ButtonProps) => {
  return <></>;
};

export default Button;
