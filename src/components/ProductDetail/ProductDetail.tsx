interface ProductDetailProps {
  imageList: string[];
  title: string;
  description: string;
  price: number;
  color: string[];
  size: string[];
  quantity: number;
  stock: number;
}
const ProductDetail = ({
  imageList,
  title,
  description,
  price,
  color,
  size,
  quantity,
  stock,
}: ProductDetailProps) => {
  return <></>;
};

export default ProductDetail;
