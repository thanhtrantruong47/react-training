interface ProductDetailProps {
  imageList: string[];
  title: string;
  description: string;
  price: number;
  color: string[];
  size: string[];
  quantity: number;
  stock: number;
}

const ProductDetail = (_: ProductDetailProps) => {
  return <></>;
};

export default ProductDetail;
