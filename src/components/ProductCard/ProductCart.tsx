interface ProductCardProps {
  image: string;
  title: string;
  price: number;
  description: string;
  rating: number;
  isActive: boolean;
}

const ProductCard = (_: ProductCardProps) => {
  return <></>;
};

export default ProductCard;
