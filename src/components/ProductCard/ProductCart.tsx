interface ProductCardProps {
  image: string;
  title: string;
  price: number;
  description: string;
  rating: number;
  isActive: boolean;
}
const ProductCard = ({
  image,
  title,
  price,
  description,
  rating,
  isActive,
}: ProductCardProps) => {
  return <></>;
};

export default ProductCard;
