interface SearchInputProps {
  placeholder: string;
  inputType: string;
  onChange: () => void;
}

const SearchInput = ({
  placeholder,
  inputType,
  onChange,
}: SearchInputProps) => {
  return <></>;
};

export default SearchInput;
