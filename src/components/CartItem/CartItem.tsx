interface CartItemProps {
  image: string;
  title: string;
  price: number;
  quantity: number;
  stock: number;
}

const CartItem = (_: CartItemProps) => {
  return <></>;
};

export default CartItem;
