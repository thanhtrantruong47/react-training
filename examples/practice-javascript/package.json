{
  "devDependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "parcel": "^2.12.0",
    "process": "^0.11.10"
  },
  "scripts": {
    "start": "rm -rf .parcel-cache && parcel src/pages/*.html",
    "build": "rm -rf .parcel-cache && parcel build src/pages/*.html"
  },
  "dependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "express": "^4.19.2"
  }
}
